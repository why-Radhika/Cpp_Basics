// First program in C++ to print "Hello World"

#include <iostream>                               //preprocessor directive used to include files in our program. The code is including the contents of the iostream file.

int main() {                                      //C++ program must have the main() function. The execution of code beings from this function.
    std::cout << "Hello World!";                  //prints the content inside the quotation marks. It must be followed by << followed by the format string.
    return 0;
}

                                                  //we need to use #include <iostream> to use cout that allows us to print output on the screen.
